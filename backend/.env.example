# Optional PostgreSQL URL. Leave unset during the initial skeleton stage.
DATABASE_URL=postgresql://user:password@localhost:5432/pulseo

# Reserved for the future AI integration. Never expose this key to the mobile app.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
